/**
 * `esg` domain types.
 *
 * PLACEHOLDER — populated by the Domains phase. Add on-chain struct mirrors,
 * numeric enums (values MUST match the Solidity `enum` order), and their
 * `Object.freeze`d `*_LABELS` maps here, mirroring the conventions in
 * `../types/core.ts`. Keep every field `readonly`; use `bigint` for
 * uint256/uint64, `number` for uint8/uint16, and the branded `Address` /
 * `Bytes32` / `Hex` types from `./core`.
 *
 * Re-exported by `../types/index.ts`. Until filled this module intentionally
 * exports nothing.
 */
export {};
