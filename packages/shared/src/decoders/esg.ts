/**
 * `esg` domain event/struct decoders.
 *
 * PLACEHOLDER — populated by the Domains phase. Add viem-log/struct decoders
 * here that validate raw on-chain shapes with zod and normalize them into the
 * immutable mirrors from `../types/esg`, following the conventions in
 * `../decoders/core.ts` (throw `ValidationError` on malformed input; never
 * silently coerce). Re-exported by `../decoders/index.ts`.
 */
export {};
