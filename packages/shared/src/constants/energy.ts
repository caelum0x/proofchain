/**
 * `energy` domain constants — access-control role ids, enum value tuples, and
 * metadata keys.
 *
 * PLACEHOLDER — populated by the Domains phase. Export `as const` values here,
 * e.g. AccessControl role hashes as branded `Bytes32`, `readonly` enum-value
 * tuples used by the matching decoders, and `keccak256` metadata keys. Keep
 * everything immutable (`as const` / `Object.freeze`). Re-exported by
 * `../constants/index.ts`.
 */
export {};
